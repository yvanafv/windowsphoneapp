<Page
    x:Class="MinhaEstante.View.NovoLivroPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MinhaEstante.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Margin="10">
        <StackPanel Orientation="Vertical" Margin="0,12,0,12">
            <TextBox Header="Nome" DataContext="{Binding SelectedLivro}" Text="{Binding Titulo, Mode=TwoWay}"/>
            <TextBox Header="Email" DataContext="{Binding SelectedLivro}" Text="{Binding Autor, Mode=TwoWay}"/>
        </StackPanel>
    </Grid>

    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton Content="Salvar" Icon="Save" Command="{Binding UpdateLivro}" 
                          CommandParameter="{Binding SelectedLivro}" 
                          Click="AppBarButton_Click"/>
        </CommandBar>
    </Page.BottomAppBar>
</Page>
